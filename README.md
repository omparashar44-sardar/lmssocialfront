# LMS Social Frontend

React/Vite frontend for the LMS app.

## Setup

1. Install Node.js.
2. Install dependencies:

```bash
npm install
```

3. Start the dev server:

```bash
npm run dev
```

The default local frontend URL is `http://127.0.0.1:5173/`.
